<UserControl x:Class="TimaivAddIn.UserControls.UserControlSettings"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <UserControl.Resources>
        <ResourceDictionary Source="pack://application:,,,/TimaivAddIn;component/Styles/SettingsStyles.xaml"/>
    </UserControl.Resources>

    <ContentControl Style="{StaticResource PaneLeftWrapper}">
        <TextBlock Style="{StaticResource PrimaryText}" Text="Settings" FontSize="36" Margin="0,0,0,0"/>

    </ContentControl>
</UserControl>